import React from 'react';
import ResumeProfile from './ResumeProfile.js';

// The top‑level application component.  It renders the ResumeProfile
// component defined in the project and could be extended in the future
// to include routing or additional pages.
export default function App() {
  return <ResumeProfile />;
}